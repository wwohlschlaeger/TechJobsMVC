<h1>@ViewBag.title</h1>

<form action="/Search/Results">
    <h2>Search by:</h2>

    <p>
        @foreach (KeyValuePair<string, string> column in ViewBag.columns)
		{
            <span>
                <input type="radio"
                       name="searchType"
                       id="@column.Key"
                       value="@column.Key" 
                       checked="@column.Key == 'all''"/>
                <label for="@column.Key">@column.Value</label>
            </span>
		}
    </p>

    <p>
        <label for="searchTerm">Keyword:</label>
        <input type="text" name="searchTerm" id="searchTerm" />
    </p>

    <input type="submit" value="Search" />
		
</form>

<h1>@ViewBag.title</h1>

@if (ViewBag.jobs != null)
{

	<h3>@ViewBag.jobs.Count Result(s)</h3>

	// TODO #2 - Loop over jobs dictionary and display all job fields
	<table>
		@for (int i = 0; i < ViewBag.jobs.Count; i++)
		{
			@foreach (KeyValuePair<string, string> job in ViewBag.jobs[i])
					{ 
				<tr class="@( i % 2 == 0 ? "even" : "odd" ) ">
					<th>@job.Key</th>
					<td> @job.Value</td>
				</tr>
					}
			<tr><td style= "padding: 20px"></td></tr>
		}
	</table>
}

<hr />

